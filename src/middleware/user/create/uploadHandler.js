const { app } = require( '@index' );
const uploadUserImage = require( '@controller/userController/fileUploadController.js' );

app.post('/api/v1/user', ( req, res, next ) => {
   // initial processing of request object
   try{

   }
   catch( err ) {
      
   }
   next();
})